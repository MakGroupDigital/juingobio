rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for development
    // WARNING: This is very permissive and should NOT be used in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
